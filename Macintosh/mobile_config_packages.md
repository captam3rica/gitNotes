# Creating Mobile Configuration Packages 

## mcxToProfile

* `./mcxToProfile.py --plist /path/to/a/plist --identifier [whatevername]`

  * Plists can be specified multiple times to package multiple configs into one larger config.
  *
